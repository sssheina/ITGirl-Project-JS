function validateModalWindow(arrayReference) {
    arrayReference.forEach(el => {
        if (el.type.value === 'Справочные материалы') {
            type.innerHTML = 'Справочные материалы';
          }
})
};